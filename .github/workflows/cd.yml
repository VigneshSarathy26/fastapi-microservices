name: deployment
on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed
    branches:
      - dev
      - stage
      - test
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4