name: deployment to ${{ github.ref_name }}
on:
  push:
    branches:
      - stage
      - test
      - dev
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4